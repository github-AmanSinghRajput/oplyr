node_modules
dist
.DS_Store
.env
.local-api-auth-token
.npm-cache
.vscode
__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/
local-models/
data/*.json
data/audio
data/generated-audio
native/
stitch_mobile_voice_console/
*.docx
# Anchored to repo root — a bare `Codex/` also matched apps/api/src/features/codex/ (case-insensitive
# on macOS), so that source dir was silently never committed and CI broke. Keep it root-only.
/Codex/
/Claude/
.claude/
LAUNCH_TRACKER.md

# Local runtime data & databases (never commit)
.local/
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite3

# Env overrides
.env.local
.env.*.local

# Build / tooling artifacts
build/
.build/
coverage/
*.log
*.tsbuildinfo
.tmp-qa-repo/

# Packaging outputs (tracked source lives in apps/desktop/assets/: icon.icns, icon-source.svg, entitlements)
apps/desktop/release/
apps/desktop/assets/icon-1024.png
apps/desktop/assets/icon.iconset/
apps/api/dist-pack/

# Internal-only docs (kept local, not published to the public repo)
docs/superpowers/
docs/LOCAL_SHARED_MEMORY_GRAPH_RESEARCH.md
docs/VIBE_MUSIC_EXPLORATION.md
docs/PROVIDER_EXECUTION_PLAN.md

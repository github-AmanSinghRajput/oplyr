<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Electron's V8 needs JIT + writable/executable memory under the hardened runtime. -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <!-- Required to load the better-sqlite3 native addon and spawn the bundled Node API / STT binary. -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <!-- The STT worker launches via a login shell that inherits DYLD/PATH env. -->
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>
    <!-- Microphone access for voice capture. -->
    <key>com.apple.security.device.audio-input</key>
    <true/>
</dict>
</plist>
